document.addEventListener("DOMContentLoaded", () => {

    console.log("Reading Housing — A Proposition in Formation");

    Mirador.viewer({
        id: "mirador-viewer",
        windows: [
            {
                manifestId: "manifest.json"
            }
        ]
    });

});